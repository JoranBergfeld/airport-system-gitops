apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: flight
    application-system: airport-system
  name: flight
  namespace: airport-system
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: flight
    weight: 100
